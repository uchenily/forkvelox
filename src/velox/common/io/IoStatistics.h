#pragma once
#include <atomic>
#include <mutex>
#include <string>
#include <unordered_map>

namespace facebook::velox::io {

// Minimal stub
class IoStatistics {
 public:
  // Add stub methods as needed by File.h or usage
  struct OperationCounters {
      // empty
  };
};

} // namespace facebook::velox::io
